# تقرير دمج الذكاء الاصطناعي في بوت التداول `trading-boti`

تم دمج قدرات الذكاء الاصطناعي في بوت التداول `trading-boti` لتعزيز دقة إشارات التداول وتحسين عملية اتخاذ القرار. يركز هذا الدمج بشكل أساسي على تحليل المشاعر (Sentiment Analysis) لفلترة الصفقات بناءً على "مزاج" السوق العام.

## 1. وحدة الذكاء الاصطناعي (AI Engine)

تم إنشاء وحدة جديدة باسم `ai_engine.py`، والتي تتولى مسؤولية التفاعل مع نماذج الذكاء الاصطناعي الخارجية. في هذه المرحلة، تم استخدام واجهة برمجة تطبيقات OpenAI (API) لتحليل المشاعر.

*   **`AIEngine` الفئة**: تقوم بتهيئة عميل OpenAI باستخدام مفتاح `OPENAI_API_KEY` الذي يتم توفيره كمتغير بيئة. إذا لم يتم توفير المفتاح، فإن وظائف الذكاء الاصطناعي لن تعمل، وسيعود البوت إلى سلوكه السابق.
*   **`get_market_sentiment` الوظيفة**: تستقبل عناوين الأخبار أو النصوص المتعلقة بالسوق، ثم تستخدم نموذج لغة كبير (مثل `gpt-4.1-mini`) من OpenAI لتحليل هذه النصوص وتحديد المشاعر العامة للسوق (إيجابية `bullish`، سلبية `bearish`، أو محايدة `neutral`). يتم إرجاع كلمة واحدة فقط كاستجابة.

## 2. دمج الذكاء الاصطناعي في منطق الاستراتيجية

تم تعديل ملف `improved_equal_levels_bot.py` لدمج وحدة الذكاء الاصطناعي في عملية اتخاذ القرار:

*   **جلب أخبار السوق (المحاكاة)**: تم إضافة وظيفة `_fetch_market_news` (في الوقت الحالي، هي وظيفة محاكاة تقوم بإرجاع عناوين أخبار عشوائية) لجلب معلومات السوق التي سيتم تحليلها بواسطة الذكاء الاصطناعي. في بيئة حقيقية، سيتم ربط هذه الوظيفة بمصادر أخبار مالية فعلية.
*   **فلترة الإشارات الأولية**: قبل أن يقوم البوت بتعيين إشارة إعادة اختبار (pending retest)، يقوم الآن بجلب أخبار السوق وتحليل مشاعرها باستخدام `AIEngine.get_market_sentiment()`. إذا كانت المشاعر سلبية (bearish) وتم رصد إشارة شراء (BUY)، يتم تجاهل الإشارة. وبالمثل، إذا كانت المشاعر إيجابية (bullish) وتم رصد إشارة بيع (SELL)، يتم تجاهل الإشارة.
*   **فلترة إعادة الاختبار (Retest)**: حتى بعد رصد إشارة أولية وتعيينها كـ "إعادة اختبار معلقة"، يقوم البوت بإعادة فحص مشاعر السوق قبل تنفيذ الصفقة الفعلية. إذا تغيرت المشاعر لتصبح معاكسة لاتجاه الصفقة، يتم إلغاء إعادة الاختبار ولا يتم فتح الصفقة.
*   **إشعارات تيليجرام**: يتم إرسال إشعارات عبر تيليجرام عندما يتم فلترة إشارة أو إلغاء إعادة اختبار بسبب مشاعر السوق، مما يوفر للمستخدم رؤية حول تأثير الذكاء الاصطناعي على قرارات التداول.

## 3. تحديث ملفات المشروع

تم تحديث الملفات التالية:

*   **`ai_engine.py`**: ملف جديد يحتوي على منطق التفاعل مع OpenAI.
*   **`improved_equal_levels_bot.py`**: تم تعديله لدمج `AIEngine` وتطبيق فلترة المشاعر على إشارات التداول وإعادة الاختبار.
*   **`bot_runner.py`**: تم تحديثه لتمرير مفتاح `OPENAI_API_KEY` إلى `EqualLevelsBot`.
*   **`fast_backtest.py`**: تم تعديله ليشمل محاكاة لمشاعر السوق (عشوائية) أثناء الاختبار الرجعي، مما يسمح بتقييم تأثير فلترة المشاعر على الأداء.

## 4. كيفية تفعيل الذكاء الاصطناعي

لتفعيل وظائف الذكاء الاصطناعي، يجب عليك توفير مفتاح `OPENAI_API_KEY` كمتغير بيئة في بيئة النشر الخاصة بك (مثل Render.com). بدون هذا المفتاح، لن يتمكن البوت من الاتصال بـ OpenAI، وستظل وظائف الذكاء الاصطناعي غير نشطة.

## 5. الخلاصة

يعمل دمج الذكاء الاصطناعي من خلال تحليل المشاعر على إضافة طبقة إضافية من الذكاء إلى بوت التداول. من خلال الأخذ في الاعتبار المشاعر العامة للسوق، يمكن للبوت تجنب الصفقات التي قد تكون محفوفة بالمخاطر في ظل ظروف سوق غير مواتية، مما قد يؤدي إلى تحسين الأداء العام وتقليل الخسائر. هذا يمثل خطوة أولى نحو دمج قدرات تعلم آلي أكثر تعقيدًا في المستقبل، مثل التنبؤ بالأسعار أو تحسين المعاملات ديناميكيًا.
